<UserControl x:Class="Mideej.Controls.ColorPickerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="40" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Color Preview Button -->
        <Button Grid.Column="0"
                Click="ColorButton_Click"
                Height="32"
                Margin="0,0,8,0"
                Cursor="Hand"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Color swatch -->
                                        <Border Grid.Column="0"
                                                Width="50"
                                                Height="26"
                                                Margin="3"
                                                CornerRadius="2"
                                                BorderBrush="{DynamicResource BorderBrush}"
                                                BorderThickness="1">
                                            <Border.Background>
                                                <SolidColorBrush Color="{Binding SelectedColor, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                            </Border.Background>
                                        </Border>

                                        <!-- Color value text -->
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding ColorText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                   VerticalAlignment="Center"
                                                   Margin="8,0"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   FontFamily="Consolas"
                                                   FontSize="{DynamicResource BaseFontSize}"/>
                                    </Grid>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource SurfaceHighlightBrush}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <!-- Pick Color Button -->
        <Button Grid.Column="1"
                Content="🎨"
                Width="32"
                Height="32"
                FontSize="16"
                Click="ColorButton_Click"
                Style="{StaticResource IconButton}"
                ToolTip="Pick color"/>
    </Grid>
</UserControl>
